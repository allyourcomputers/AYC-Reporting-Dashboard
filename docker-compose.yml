version: '3.8'

services:
  halo-reporting:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: halo-reporting
    restart: unless-stopped
    ports:
      - "${PORT:-3100}:3100"
    environment:
      # HaloPSA API Configuration
      - HALO_API_URL=${HALO_API_URL}
      - HALO_CLIENT_ID=${HALO_CLIENT_ID}
      - HALO_CLIENT_SECRET=${HALO_CLIENT_SECRET}

      # Supabase Configuration
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}

      # NinjaOne Configuration
      - NINJA_CLIENT_ID=${NINJA_CLIENT_ID}
      - NINJA_CLIENT_SECRET=${NINJA_CLIENT_SECRET}
      - NINJA_BASE_URL=${NINJA_BASE_URL}

      # 20i API Configuration
      - TWENTYI_API_KEY=${TWENTYI_API_KEY}
      - TWENTYI_OAUTH_KEY=${TWENTYI_OAUTH_KEY}

      # Server Configuration
      - PORT=3100
      - NODE_ENV=production
    env_file:
      - .env
    networks:
      - halo-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3100/api/config', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  halo-network:
    driver: bridge
